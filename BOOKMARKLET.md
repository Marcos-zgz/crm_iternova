javascript:(async function(){const API_URL='https://crm-iternova.pages.dev/api/expedientes';const regexExp=/\bE-\d{3,5}-\d{2}\b/i;const regexCarreteraAragon=/\b(A-\d{1,4}|ARA-A\d{1,2}|ARA-\d{1,3}|AC-\d{1,3}|ZC-\d{1,3})\b/i;function buscarValor(etiquetas){const todos=Array.from(document.querySelectorAll('td, th, label, div, span, b, strong'));for(let el of todos){const t=el.innerText?.trim()||'';for(let eti of etiquetas){if(t.toLowerCase()===eti.toLowerCase()||t.toLowerCase().startsWith(eti.toLowerCase()+':')){if(el.nextElementSibling){const val=el.nextElementSibling.innerText?.trim()||el.nextElementSibling.querySelector('input,select,textarea')?.value?.trim();if(val)return val;}if(el.parentElement&&el.parentElement.children.length>1){const arr=Array.from(el.parentElement.children);const idx=arr.indexOf(el);if(idx!==-1&&arr[idx+1]){const val=arr[idx+1].innerText?.trim()||arr[idx+1].querySelector('input,select,textarea')?.value?.trim();if(val)return val;}}}}}return'';}let cod='';const inputs=Array.from(document.querySelectorAll('input, select, textarea'));for(let el of inputs){if(el.value&&regexExp.test(el.value)){cod=el.value.match(regexExp)[0];break;}}if(!cod){const txts=Array.from(document.querySelectorAll('h1,h2,h3,h4,th,td,b,strong,span,div'));for(let el of txts){if(el.children.length===0&&regexExp.test(el.innerText)){cod=el.innerText.match(regexExp)[0];break;}}}if(!cod){cod=prompt('Introduce código expediente (ej: E-1096-26):');if(!cod||!cod.trim())return;cod=cod.trim();}const urlIter=window.location.href.includes('codeID=')?window.location.href:(document.querySelector('iframe')?.src||'');const titulo=buscarValor(['Titulo del expediente','Título del expediente','Asunto']);let carretera=buscarValor(['Carretera','Tipo de carretera','Vía','Denominación','Matrícula vía','Tramo']);if(!carretera||!regexCarreteraAragon.test(carretera)){const matchAragon=document.body.innerText.match(regexCarreteraAragon);if(matchAragon)carretera=matchAragon[0].toUpperCase();}const pk=buscarValor(['PK inicial','P.K. inicial','PK','Punto kilométrico']);let locSugerida=[carretera,pk?`PK ${pk}`:''].filter(Boolean).join(' - ');const locFinal=prompt(`Ubicación confirmada (Carretera y PK):`,locSugerida||'A- - PK ');if(!locFinal)return;let coordsExtra='';const lkCoord=Array.from(document.querySelectorAll('a, iframe, embed')).find(el=>(el.src||el.href||'').includes('viewpoint='));if(lkCoord){const mCoords=(lkCoord.src||lkCoord.href).match(/viewpoint=([0-9.-]+),([0-9.-]+)/);if(mCoords){coordsExtra=` | ${mCoords[1]},${mCoords[2]}`;}}const locParaGuardar=locFinal.trim()+coordsExtra;let fRegStr=buscarValor(['Fecha de registro','Fecha de expediente','Fecha']);let fBase=new Date();if(fRegStr&&fRegStr.includes('/')){const p=fRegStr.split(' ')[0].split('/');if(p.length===3)fBase=new Date(p[2],p[1]-1,p[0]);}const tipoTramite=prompt(`Expediente: ${cod}\n\nTipo de plazo:\n1 = Normal (90 días)\n2 = Ayuntamiento (30 días)`,'1');const diasPlazo=(tipoTramite==='2')?30:90;const fLimiteObj=new Date(fBase.getTime()+(diasPlazo*24*60*60*1000));const fLimite=fLimiteObj.toISOString().split('T')[0];const colPrompt=prompt(`Columna:\n1 = Requerimientos\n2 = Visita de campo\n3 = Contactar interesado\n4 = En su tejado`,'1');const estados={'1':'requerimiento','2':'visita_campo','3':'contactar_interesado','4':'informe_externo'};const estado_bloqueo=estados[colPrompt]||'requerimiento';const notas=prompt('Nota interna / atasco (opcional):','');const aviso=document.createElement('div');aviso.innerText=`⏳ Guardando ${cod}...`;Object.assign(aviso.style,{position:'fixed',top:'20px',right:'20px',zIndex:999999,background:'#1e293b',color:'#fff',padding:'12px 18px',borderRadius:'8px',fontSize:'13px',boxShadow:'0 4px 12px rgba(0,0,0,0.15)',fontFamily:'sans-serif'});document.body.appendChild(aviso);try{const payload={codigo:cod,titulo:titulo||'',localizacion:locParaGuardar,interesado:'',fechaRegistro:fBase.toISOString().split('T')[0],estado_bloqueo:estado_bloqueo,fecha_limite:fLimite,notas:notas?notas.trim():'',url_iternova:urlIter};const res=await fetch(API_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(res.ok){aviso.style.background='#16a34a';aviso.innerText=`✅ ${cod} guardado`;}else{throw new Error(`Error ${res.status}`);}}catch(e){aviso.style.background='#dc2626';aviso.innerText=`⚠️ ${e.message}`;}setTimeout(()=>aviso.remove(),4000);})();
